import React, { useMemo } from 'react';

// import layout
import Layout from 'layout/Layout';

import Vertical from "./views/Vertical";

const App = () => {
  return (
      <Layout>
        <Vertical/>
      </Layout>
  )};

export default App;
